{
  "name": "app-model",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "android:prebuild": "expo prebuild --platform android",
    "android:bundleRelease": "set NODE_ENV=production&& npx expo prebuild --platform android && cd android && gradlew.bat bundleRelease",
    "android:aab": "set NODE_ENV=production&& npx expo prebuild --platform android && cd android && gradlew.bat bundleRelease",
    "ios": "expo run:ios",
    "ios:bundleRelease": "eas build --platform ios --profile production",
    "build:release": "npm run android:bundleRelease && npm run ios:bundleRelease",
    "web": "expo start --web",
    "bundle": "expo export --output-dir dist",
    "build": "npx expo export --output-dir ./dist"
  },
  "dependencies": {
    "expo": "~54.0.0",
    "expo-asset": "~12.0.7",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "babel-preset-expo": "~54.0.0",
    "@react-native-community/cli": "latest"
  },
  "private": true
}